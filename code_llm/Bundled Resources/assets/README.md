This folder holds small templates and example configs used by the `code_llm` skill.

Files to include:
- `requirements.txt` — minimal Python deps for running example scripts.
- `prompt_templates.md` — example instruction tuning prompts and evaluation prompts.
- `example_config.yaml` — sample training config fields (batch_size, lr, warmup_steps, etc.).

Edit or extend these assets to match your project's tooling.
